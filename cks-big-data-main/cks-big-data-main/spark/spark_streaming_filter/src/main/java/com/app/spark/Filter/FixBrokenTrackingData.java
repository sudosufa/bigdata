package com.app.spark.Filter;

import com.app.spark.model.Tracking;

public class FixBrokenTrackingData {
/*
    public Tracking FixBrokendata(Tracking tracking, Tracking trackingPrevious) {
       if (tracking.getlVCAN_FUEL_LEVEL() == 0 && trackingPrevious != null && trackingPrevious.getCan_fuel_level() > 0) {
            tracking.setlVCAN_FUEL_LEVEL(trackingPrevious.getCan_fuel_level()) ;

        }
        if (tracking.getlVCAN_FUEL_CONSUMED() == 0 && trackingPrevious!=null && trackingPrevious.getFuel_consumed() > 0) {
            tracking.setlVCAN_FUEL_CONSUMED(trackingPrevious.getFuel_consumed()) ;
        }
        if (tracking.getlVCAN_TOTAL_MILEAGE() == 0&& trackingPrevious!=null && trackingPrevious.getTotal_mileage() > 0) {
            tracking.setlVCAN_TOTAL_MILEAGE(trackingPrevious.getTotal_mileage()) ;
        }
        return tracking;
    }*/
}
